{{#unless (wasKindUsed reflection.kindString)}}
    </ul>
    <h3{{#if @first}} class="first"{{/if}}>{{useKind reflection.kindString}}</h3>
    <ul>
{{/unless}}
{{#if isInPath}}
    </ul>
    <ul class="current">
{{/if}}
<li class="{{#if isInPath}}current{{/if}} {{cssClasses}}">
    <a href="{{relativeURL url}}" class="tsd-kind-icon">{{{wbr title}}}</a>
    {{#if children}}
        <ul>
            {{#each children}}
                {{> toc}}
            {{/each}}
        </ul>
    {{/if}}
</li>
{{#if isInPath}}
    </ul>
    <ul class="after-current">
{{/if}}
